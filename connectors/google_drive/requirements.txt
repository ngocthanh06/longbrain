# Host-side only (see README.md) — the llamaindex-service container never
# needs these; the connector talks to it over plain HTTP like every other
# host-side script (ingest_watcher.py, hooks/*).
google-auth==2.36.0
google-auth-oauthlib==1.2.1
google-api-python-client==2.154.0
