# Roadmap

What's live, what's next. Historical design details of completed phases are
preserved in git history (`UPGRADE_PLAN.md`, removed 2026-07-10).

## Completed milestones

| When | Milestone |
|---|---|
| 2026-07-05 | **v2 core + project partitioning** — four memory layers, deterministic ids, dedup/supersede, per-project scoping. |
| 2026-07-05 | **Phase A — automatic lifecycle**: consolidation on session end + 30-min idle sweep; recall auto-injected into every turn (`pre_llm_call`); memory management (list/forget REST + MCP). |
| 2026-07-05 | **Phase B — automated backup**: launchd nightly at 2:00 AM, 7-copy retention. |
| 2026-07-07 | **Phase D — docs/ auto-ingest watcher**: stdlib-only 60s poll over each project's `docs/` folder, duplicate-guarded; works on Claude-Code-only machines via the adapter-maintained project catalog. |
| 2026-07-09 | **Recall eval harness** (`scripts/recall_eval.py` + baseline) — recall quality is now measured, not guessed; rule-based memory router (docs channel on trigger words); session summaries in consolidation; adapter SDK docs + minimal example. |
| 2026-07-10 | **Phase C — recall quality, closed**: C1 embedding swap **rejected by measurement** (two blind LLM-judged benchmarks vs `multilingual-e5-large` below the 1.3× decision bar; MiniLM stays). C2 **hybrid BM25 shipped**: sparse exact-token rescue channel on all three collections, gated to identifier-like queries; exact-token hit@top-2 went 1/12 → 11/12 with byte-identical results for prompts without such tokens. Kill switch `HYBRID_BM25=false`. |
| 2026-07-10 | **Identity cleanup**: hermes → longbrain rename across MCP server, containers, image, env (legacy `HERMES_*` aliases kept), Qdrant collections (migrated 1144/1144 points); `/ui` named-vector fix; preference boost in recall (`RECALL_PREFERENCE_BOOST`, trap-tested in the eval set). |
| 2026-07-11 | **v0.1.0 (beta / public preview)**: CI on GitHub Actions with the recall eval as a required gate; README states eval-backed numbers and explicit known limitations; `llms.txt`; Codex write adapter shipped. |
| 2026-07-11 | **Codex lifecycle adapter**: official `SessionStart`, `UserPromptSubmit`, and `Stop` hooks add automatic recall injection, turn recording, and consolidation catch-up; rollout-scanning `notify` retained as a compatibility fallback. |
| 2026-07-11 | **KG-lite — triple-based supersession + entity graph**: facts may carry a `(subject, relation, object)` triple; a new fact sharing subject+relation with an active one retires it, catching plain contradictions (pnpm → bun) that cosine similarity and the LLM dedup band both miss. Kill switch `TRIPLE_SUPERSEDE=false`. Adds `/memory/graph?mode=entities` + an Entities view in `/ui`, and `scripts/backfill_triples.py` (dry-run writes a plan file; `--apply` writes exactly that plan, zero new LLM calls). |
| 2026-07-10 | **Agent support tiers + doctor**: formal tier model (full adapter / write adapter / MCP-only / generic MCP) documented in `adapters/README.md`; Codex wired with MCP + turn-ended notify recording (`scripts/configure_codex.py`, `hooks/codex/turn_ended.py`, detected by setup.sh); `scripts/doctor.py` — one-shot read-only wiring + health check across service, launchd jobs and all agents, with `--fix` re-running setup. |
| 2026-07-11 | **Memory quality-of-life batch**: `last_seen` refreshes a fact's decay clock on recall (`LAST_SEEN_REFRESH`); task-type facts get an open/done `status` (`HIDE_DONE_TASKS`); a contradiction detector flags conflicting facts via `conflicts_with` instead of silently keeping both (`CONTRADICTION_DETECTION`); a memory health dashboard (`GET /memory/stats` + `/ui` panel: counts, superseded ratio, 24h/7d growth, consolidation backlog); `/ui` graph gets topic sub-clustering via connected-components over existing similarity data (`GRAPH_TOPIC_CLUSTERING`, zero new LLM calls). |
| 2026-07-11 | **KG-lite follow-up audit**: closed the 2 open items from the original KG analysis — added a genuine multi-hop eval case (`vn-multihop-project-deploy`, confirmed failing on purpose — the entity graph is `/ui`-visualization-only, not wired into recall) and deleted the 2 specific meta-about-system facts the earlier data audit had flagged but never removed. Separately, root-caused and fixed Codex Desktop's per-chat scratch cwd (`~/Documents/Codex/<date>/<title-slug>`) being persisted as a junk project — `resolve_project` now recognizes and skips that layout. |
| 2026-07-17 | **Document search upgrade** ([SEARCH_SPEC](SEARCH_SPEC.md)): Sprint 0 eval set (38 vague-VN → JA/EN cases from the real corpus) gated the decision — BGE-M3 doc-level hit@3 0.816 vs MiniLM 0.447 (1.83×, bar 1.3×). Shipped: separate doc embedder (`DOC_EMBED_*`, documents collection ONLY — memories/history stay on MiniLM, distillation thresholds untouched), `huggingface` provider path (fastembed has no bge-m3), doc-space meta guard, 384→1024d migration (`scripts/migrate_doc_embed.py`), typed 503 `doc_embedder_unavailable` error contract + `/health` readiness, cross-encoder reranker behind `DOC_RERANK`, and the Ollama-gated Optional tier (AI summary chunks via `DOC_LLM_*`/`DOC_ENRICH`, on-demand `/query/explain` with coarse labels — no fake confidence %). **Gate passed ≠ KPI met**: hit@3 is 0.816 offline / 0.842 service-path vs the spec KPI ≥ 0.90 — Sprints 0–2 accepted, KPI still open (planned lever: re-run the eval with Sprint 3 enrichment active). |
| 2026-07-28 | **Phase 1 — Memory Reliability, closed** ([STRATEGY.md](STRATEGY.md)): stable `document_key` (relative path, not basename) drives supersede-on-reingest for L4 (686→164 legacy pre-key chunks repaired, 0 left active/visible); recency decay for document search (`DOC_HALF_LIFE_DAYS`); item-level context budgeting (`CONTEXT_BUDGET_CHARS`, never cuts an item/header mid-line, replacing a blind character slice); a `trace` field on every recall hit (similarity/decay/boost breakdown for debugging ranking, not shown to agents); `hooks/admission_gate.py` replacing 3 copy-pasted prompt-length/context-cap gates across Hermes/Claude Code/Codex. **Verified live** (full rebuild + restart, not just unit tests): 0 data loss across restart, re-sync produces 0 duplicates, a live ingest-v1-then-v2 smoke test confirmed correct supersession, project isolation holds under an adversarial same-query-different-project test, 0% stale-chunk leakage (queried verbatim text from a superseded chunk — only the active version came back). **Baseline**: recall hit rate 12/13 (92%, fixture eval, unchanged — known multihop gap only); context size ~880 chars/case (eval avg) and ~3.2–3.5k chars on broad live queries, always under the 6000-char budget; 0 duplicate/version-conflict groups across 98 keyed documents; recall latency ~275ms warm (SEARCH_SPEC's <500ms budget). **Known residual**: 164 chunks predating `document_key` still lack the tag (0 are active/visible — harmless) but `scripts/migrate_document_versions.py` now hard-blocks (exit 2) until they're repaired; fixing them needs manual per-group review, not automation (basename-guessing across ambiguous groups is what caused the earlier gakken/rz-server rollback). 213 tests passing. |
| 2026-08-07 | **Security hardening, Phase 0/2/3/4/5 (local-only threat model)**: confirmed deployment target is local-only (Qdrant + this API already bind 127.0.0.1-only; matches the "no multi-user/sync" non-goal below) — Phase 1 (auth) not yet built at this point, revisited and shipped as optional defense-in-depth in the 2026-08-12 entry below. `forget_session` (MCP + REST) now requires explicit confirmation, matching `forget_memory`/`forget_everything`. All Qdrant-payload-driven file deletes (`delete_document`, `delete_document_path`, `cleanup_superseded`) route through the one `document_path()` boundary helper — no more raw `Path(stored_path).unlink()`; covered by new traversal/absolute-path/symlink-escape/malicious-payload tests. Added size/count caps (`MAX_FACT_TEXT_CHARS`, `MAX_FACTS_PER_CALL`, `MAX_TURN_TEXT_CHARS`, `MAX_KB_TEXT_CHARS`, `MAX_INGEST_FILE_BYTES`, `MAX_INGEST_PATH_FILES`) and an in-process rate limiter (`app/ratelimit.py`, no new dependency) on every write/destructive path (`memory_append`, `save_memories`, `add_to_knowledge_base`, ingest, `cleanup_garbage`) — bounds a runaway/looping agent, not a per-user quota (there is only one user). Structured audit log lines (`logger.info("audit action=... allow=...")`, ids/counts only, never content) on every destructive tool/endpoint. **Deferred, not forgotten**: TLS and quarantine-for-untrusted-ingest — no caller/threat needs them under local-only; production `docker-compose` split (drop `--reload`) and a `/documents/path` source allowlist — would break the intended single-user convenience (import any file from your own disk) without adding real protection on a single trusted machine; Phase 6 (staged/canary rollout) — doesn't apply to a local single-user tool. 185 app-level tests passing (added direct coverage for `app/ratelimit.py` and the new pydantic size/count guards; 6 host-side hook/adapter test files require repo-root `PYTHONPATH`, not collectible from inside the container — pre-existing, unrelated). |
| 2026-08-12 | **Optional shared-secret auth** (initial rollout; the Hermes header limitation described here was corrected by the 2026-08-14 entry below). The local-only threat model remains primary: `LONGBRAIN_API_KEY` in `.env`, empty by default; when set, an ASGI middleware requires `X-API-Key` on REST + `/mcp` except `GET /health`, while `/ui` uses HTTP Basic Auth. Hooks and Claude/Codex registrations were wired as part of this rollout. |
| 2026-08-13 | **Auth silent-failure hardening**: `hooks/api_auth.py` previously read `LONGBRAIN_API_KEY` from process environment only — a hook subprocess spawned by Claude Code/Codex/Hermes Desktop does not inherit the shell that ran `setup.sh`, so the header would silently never be sent. It now falls back to reading the key straight from the repo's `.env` file. `scripts/doctor.py` now flags the analogous risk for Codex's native MCP client: `env_http_headers` makes the `codex` process read the key from *its own* launch environment, which this repo cannot guarantee — doctor warns instead of leaving it a silent 401. |
| 2026-08-14 | **Tiered rate limits** (security hardening plan, Phase 3): the single flat `RATE_LIMIT_WINDOW_SECONDS`/`RATE_LIMIT_MAX_CALLS` pair either throttled normal fact-saving or left destructive actions too permissive at one shared budget. `app/ratelimit.py` now maps each write/destructive action to its own calls-per-minute tier (`memory_append` 90/min, `save_memories` 20/min, `consolidate_session` 10/min, ingest/`add_to_knowledge_base` 10/min, deletes/`cleanup_garbage` 5/min, `forget_everything` 1/min — see [docs/API.md](API.md#mcp-tools)); anything not listed (all read tools, plus any future untiered action) keeps the old flat pair as a fallback, so this is purely additive. Also closed a gap found while tiering: `consolidate_session` (MCP tool, `/memory/consolidate`, `/memory/consolidate-pending`) had no rate limit at all before this — now on the same tier as everything else that triggers consolidation. 12 new tests in `test_ratelimit.py` (275 total, up from 263). |
| 2026-08-14 | **Review fixes on the auth/rate-limit work above** (3 P1, 5 P2, all from an independent code review): **(P1)** `setup.sh` ran `docker compose up -d --build` *before* the Hermes-Desktop-vs-auth guard — the container could already be enforcing auth, breaking Hermes' MCP, before setup noticed and aborted; the guard now runs immediately after sourcing `.env`, before any runtime state changes. **(P1)** `scripts/doctor.py`'s Codex check only read its own process environment, which proves nothing about the `codex` process's launch environment and would falsely read as fine when doctor is run from a shell that happens to have the var; it now makes a real authenticated request against the running server to verify the `.env` key itself is valid, and states the launch-environment question as a separate, unverifiable caveat rather than conflating the two. **(P1)** `hooks/api_auth.py` split .env values on a bare `=` with no dotenv-format handling — `LONGBRAIN_API_KEY="secret"` (valid, and what Docker Compose's own env-file parsing accepts) sent the literal quoted string as the header, a permanent silent 401; it now strips matching quotes, inline comments, an optional `export ` prefix, and takes the last of duplicate assignments. **(P2)** `docker-compose.yml` never passed the 6 new `RATE_LIMIT_*_PER_MIN` tier variables through to the container, so `.env` overrides for them were silently ignored — added. **(P2)** rate-limiting `/memory/consolidate`'s background path (and `/memory/consolidate-pending`) raised `429`, contradicting that code's own "never block or error the caller" comment that hooks rely on; a rate-limited background call now returns `200 {"status": "skipped", "reason": "rate_limited"}` instead — the debounced catch-up sweep still reaches it later. **(P2)** `config.py` and `docs/API.md` still described the pre-Basic-Auth `/ui` design (exempt from auth, key injected into JS) after the 2026-08-12 rewrite changed both; corrected to match `main.py`'s actual `_AUTH_EXEMPT_PATHS = {"/health"}`. **(P2)** write paths (`memory_append`, `save_memories`, `add_to_knowledge_base`, ingest, `consolidate_session`) were documented as audited but `_audit()` was never actually called on them — added audit calls (rate-limit refusal and successful completion) to both the MCP tools and their REST equivalents in `main.py`/`mcp_server.py`. **(P2)** README/API.md implied destructive-tool confirmation was uniform; documented that it isn't — `DELETE /memory/facts/{id}` and `/documents/delete` have no confirmation param (unlike their MCP twin `forget_memory` and unlike every other REST delete, which do require one), by design for a human-operated REST surface, now stated as such instead of left ambiguous. 8 new tests in `test_api_auth.py` for the dotenv parsing (283 total, up from 275). |
| 2026-08-14 | **Second review pass, 3 more P1 + 3 more P2 on the same auth work**: **(P1)** `scripts/doctor.py`'s Codex auth check was entirely skipped (`if ENV_HEADER_LINE in text`, no `else`) when `.env` had a key but the Codex config predated it, so doctor reported "MCP registered" while the connection would actually 401 — added the missing branch. **(P1)** the auth probe treated any `200` with the `.env` key as success, which a server not enforcing auth at all would also return for any key; it now requires an unauthenticated request to get `401` (proves enforcement is on) before trusting a `200` on the authenticated one (proves the key matches) — four distinct outcomes (`ok`/`not_enforced`/`key_mismatch`/`unreachable`) instead of a boolean. **(P1)** `hooks/api_auth.py` checked process environment *before* the repo's `.env`, so a stale env var (e.g. a shell profile that still exports an old key) would silently outrank a rotated key in `.env`; flipped so `.env` — even an explicit empty assignment, meaning "disabled" — is authoritative, and process env is only a fallback when `.env` has no `LONGBRAIN_API_KEY` line at all. **(P2)** the dotenv parser still didn't match Compose's actual rules: `KEY=abc#123` was truncated to `abc` (Compose/dotenv only treat `#` as a comment when preceded by whitespace) and `KEY="secret" # comment` returned `"secret"` including the quote characters (the quote-matching only checked first-char==last-char, which breaks once there's trailing text after the closing quote) — rewritten to find the actual closing quote and only strip whitespace-prefixed `#`. **(P2, still open)** `.env.example` remains unedited — blocked by the `protect-env` policy on every attempt (Edit tool); the exact block to paste has been handed to the user three times now. **(P2)** added the regression tests these findings pointed out were missing: `test_setup_sh.py` (static-asserts the Hermes-vs-Docker ordering so it can't silently regress) and `test_doctor.py` (all 4 probe outcomes + the header-gap branch, via dependency-injected fakes — no real network/filesystem touched). 297 tests total, up from 283. |
| 2026-08-14 | **Correction: Hermes Desktop DOES support MCP headers** — the four prior entries above assumed otherwise (`scripts/configure_hermes.py` refused to enable auth when `~/.hermes` existed; `setup.sh` had a matching preflight abort). Verified by reading the checked-out `hermes-agent` source directly: `tools/mcp_tool.py`'s `_connect_server` reads a literal `headers: {name: value}` dict off an `mcp_servers.*` entry and attaches it to every real MCP connection (not just `hermes mcp test`); HTTP MCP auth was added there 2026-07-16 (`e0e7cfa67`), before this repo's auth rollout even started. `configure_hermes.py` now writes `headers: {"X-API-Key": ...}` on the `mcp_servers.longbrain` entry when `LONGBRAIN_API_KEY` is set (preserving any other keys already on that entry, e.g. `tools:`) and `chmod 0600`s `config.yaml` once it holds the literal key; the `setup.sh` preflight abort and the `configure_hermes.py` refusal are both removed, along with the `test_setup_sh.py` regression test that asserted the (now-incorrect) ordering requirement. `docs/API.md`'s Auth section corrected to match. |
| 2026-08-14 | **Cleanup action in `/ui`**: a "Cleanup" button previews (via `/maintenance/cleanup` dry-run) which superseded document chunks and files it would remove — grouped by project and file-kept-vs-removed — before a second, explicit confirm actually deletes them. |
| 2026-08-14 | **Codex launch-environment fix**: Codex's `env_http_headers` reads `LONGBRAIN_API_KEY` from *its own* launch environment, and a one-shot `launchctl setenv` (the prior fix) doesn't survive logout/reboot. `scripts/sync_codex_launch_env.py` + a new `com.longbrain.codex-env` LaunchAgent re-sync it into launchd's env at every login; `configure_codex.py` runs the sync immediately too, and `doctor.py` now verifies both the launchd env value and the LaunchAgent are actually in place instead of just noting they can't be checked. |
| 2026-08-14 | **v0.3.0 released** — search upgrade, memory-reliability phase, security hardening (auth + tiered rate limits), and the two entries above. 303 tests passing. |
## Next (in order)

1. **Repair the 164 pre-key legacy chunks** — manual per-group review (listed
   in the Phase 1 closing session), not automatable safely. Unblocks
   `migrate_document_versions.py`.
2. **Phase 2 — first connector** (see [STRATEGY.md](STRATEGY.md)): reuse the
   existing `documents.py` ingest shape (no new `NormalizedDocument`
   abstraction yet — STRATEGY.md's own guardrail is no shared shape before
   2+ divergent connectors exist). Cheapest-first order agreed: Folder
   (already ~done via `ingest_watcher.py`, just needs to emit the shape
   explicitly) then Markdown URL (no auth, first real local-vs-remote
   divergence), before Google Docs/Apidog (real auth complexity).

## Further out

- **Codex session-end completion** — automatic recall and recording now use
  official lifecycle hooks. Move consolidation from next-session catch-up to
  chat-close when Codex exposes a `SessionEnd` event. A Cursor adapter would
  prove the contract on another agent nobody tuned for.
- **"New machine in 10 minutes"** — a packaged restore experience around the
  transfer bundle: install, import, keep working.
- **Identifier-gated BM25 on the document search path** — `/query` and MCP
  `search_knowledge_base` are dense-only by design today; the BM25 rescue
  channel exists only in the recall router (SEARCH_SPEC constraint 5,
  clarified 2026-07-17). Wiring the same rescue into those paths would help
  identifier queries (e.g. a ticket ID) but needs an identifier-query eval
  first — same "measured, not asserted" gate as any search change.
- **ONNX doc embedder** — replace the sentence-transformers/torch stack with
  an ONNX bge-m3 (fastembed custom model or optimum export): would shrink
  the doc-search image by ~5-6GB and likely speed up CPU inference 2-4×.
  Requires verifying the exported model reproduces the accepted eval numbers
  (same gate as any vector-space change).

## Deliberate non-goals

Decided and not up for re-litigation without new evidence:

- **No multi-user / sync** — single-user, local-first by design
  (privacy-first positioning).
- **No CLOUD LLM at document-ingest time** — narrowed 2026-07-17: optional
  ai-summary enrichment now exists, but strictly via a local LLM
  (`DOC_LLM_*`, Ollama) and strictly optional — no API key ever required,
  ingest works identically without it (SEARCH_SPEC Sprint 3).
- **No embedding model swap for MEMORY collections** — measured twice,
  inconclusive both times; MiniLM stays for facts/history (its dedup/graph
  thresholds are calibrated to it). The DOCUMENTS collection moved to
  BGE-M3 on 2026-07-17 after clearing the 1.3× bar on its own eval set —
  exactly the "new evidence" this section demands.
- **No adapter-registry abstraction, no generic wrapper/proxy** — one
  `configure_<agent>.py` per agent called from setup.sh *is* the registry;
  merging them (or building a universal CLI wrapper for hook-less agents)
  waits for real demand, not aesthetics (decided 2026-07-10).
